const addTask = document.getElementById("add-to-list");
const displayTask = document.getElementById("show-list");
const taskDisplay = document.getElementById('taskDisplay');
const otherTaskLists = document.getElementById("other-lists");

